"""Agent-definition discovery owned by the legacy runtime assembly layer.

The model-facing ``Agent`` delegation tool is product-owned and is deliberately
not re-exported from here; doing so would invert the Product dependency.
"""

__all__: list[str] = []
